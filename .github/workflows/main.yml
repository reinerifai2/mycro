name: CI Demo

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run simple test
        run: echo "GitHub Action is working ✅"
run: |
  if [ -f wrongfile.txt ]; then
    echo "File missing or wrong!"
    exit 1
  fi
