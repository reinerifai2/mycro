name: CI Demo

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run simple test
        run: echo "GitHub Action is working ✅"

      - name: Check for Untitled-1.html
        run: |
          if [ ! -f Untitled-1.html ]; then
            echo "File missing or wrong!"
            exit 1
          fi
